<a href="/admin" class="return">Retour à l'admin</a>
<div class="margin-left-50 padding-top-20">
  <h1>Créer un produit à vendre</h1>
</div>

<div id="contentEdit">



<form method="post" class="form-horizontal" action="<%= @action %>" enctype="multipart/form-data">

  <% if flash[:error] %>
  <div class="alert alert-danger">
    <%= flash[:error] %>
  </div>
  <% end %>
  <% if flash[:success] %>
  <div class="alert alert-success">
    <%= flash[:success] %>
  </div>
  <% end %>

  <% [["name", "Nom", @product.name, cookies[:name], "inputNom", "text", "Nom"],
      ["desc", "Description", @product.desc, cookies[:desc], "inputDesc", "text", "Description"],
      ["more_desc", "Plus d'infos", @product.more_desc, cookies[:more_desc], "inputMore", "textarea", "Plus d'infos"],
      ["image", "Image", @product.image, cookies[:image], "inputImage", "file"]].each do |name, label, variable, cookie, id, type, placeholder| %>

    <% unless @edition and name=="image" %>
    <div class="form-group">
      <label for="<%= id %>" class="col-sm-2 control-label"><%= label %></label>
      <div class="col-sm-10">
        <% if type!="textarea" %>
          <input type="<%= type %>" class="form-control" id="<%= id %>" name="<%= name %>" placeholder="<%= placeholder %>" value="<%= variable||cookie %>">
        <% else %>
          <textarea class="form-control" id="<%= id %>" rows="7" name="<%= name %>"><%= variable||cookie %></textarea>
        <% end %>
      </div>
    </div>
    <% end %>

  <% end %>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-default">Go !</button>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <a href="/admin/product" class="btn btn-default">Annuler</a>
    </div>
  </div>


</form>

</div>

